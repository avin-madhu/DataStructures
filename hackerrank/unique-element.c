#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main() {
   
   int len;
   char string[200];
   scanf("%s",&string);
   len = strlen(string);
   
   for(int i=0;i<=len;i++)
   {
      
   }
    return 0;
}
